//largerFun.cpp
#include <iostream>
using namespace std;
#include <string>
int largerInt(int, int);
double largerDouble(double, double);
string largerString(string, string);

int main()
{
    int res = largerInt(10, 20);
    cout << "Larger of (10, 20) is " << res << endl;
    double rest = largerDouble(66.6, 88.8);
    cout << "Larger of (66.6, 88.8) is " << rest << endl;
    string st = largerString("Porsche", "Maserati");
    cout << "Larger of (\"Porsche\", \"Maserati\") is " 
¡@¡@¡@¡@¡@<< st << endl;
    return 0;
}

int largerInt(int a, int b)
{
    return a > b ? a : b;
}

double largerDouble(double c, double d)
{
    return c > d ? c : d;
}

string largerString(string s, string t)
{
    return s > t ? s : t;
}
